# metahub-site

Editable static version of the MetaHub Home page (single-file).

## Preview
```bash
node serve.mjs   # http://localhost:4321
```
Or just open via GitHub Pages.

Everything lives in `index.html` (inline CSS + vanilla JS). Swap real images into `assets/` at the same paths.

